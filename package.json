{
  "name": "kiwituri",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd server && npm install && npm install --only=dev",
    "start": "cd server && npm start"
  },
  "dependencies": {
    
  }
}
