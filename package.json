{
  "name": "kiwituri",
  "version": "1.0.0",
  "scripts": {
    "install-ui": "cd ui && npm install && npm install --only=dev",
    "install-server": "cd server && npm install && npm install --only=dev",
    "postinstall": "npm run install-ui && npm run install-server",
    "prestart": "cd ui && npm run build-prod",
    "start": "cd server && npm start"
  },
  "dependencies": {
    
  }
}
